# SvelteKit frontend — runtime env (committable template)
# Copy to .env and fill in real values. Variables prefixed PUBLIC_ are exposed
# to the browser bundle — never put server secrets here.

PUBLIC_API_URL=http://localhost:3001
PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
PUBLIC_SUPABASE_ANON_KEY=sb_publishable_XoRmwfQhLsfegeP_KLFh6w_gliXmqRT
